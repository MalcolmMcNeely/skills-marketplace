apiVersion: 1

datasources:
  - name: Loki
    uid: claude-loki
    type: loki
    access: proxy
    # Container-to-container over the podman network created by up.ps1.
    url: http://claude-loki:3100
    isDefault: true
    editable: true
